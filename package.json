{
  "name": "veripanen",
  "private": true,
  "version": "0.1.0",
  "description": "VeriPanen — AI harvest grading + on-chain escrow (BNB Smart Chain Testnet)",
  "scripts": {
    "install:all": "npm --prefix agent install && npm --prefix frontend install",
    "dev": "npm --prefix frontend run dev",
    "dev:agent": "npm --prefix agent start",
    "dev:frontend": "npm --prefix frontend run dev",
    "build": "npm --prefix frontend run build",
    "test:contract": "cd contract && forge test",
    "test:agent": "npm --prefix agent test",
    "anvil": "anvil"
  }
}
